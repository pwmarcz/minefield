Required packages (as of Ubuntu 12.04):
python-dev
libevent-dev
libpcre3-dev

Install Python libraries:

  $ pip install -r requirements.txt

Recompile SCSS:

  $ cd server
  $ make

(During development, `make watch` should recompile SCSS on change.)

Run server:

  $ cd server
  $ python server.py
